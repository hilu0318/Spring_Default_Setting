<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org/DTD Mapper 3.0/EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
	<select id="select" resultType="hash">
	SELECT 'CONNECT' 	AS NAME
	     , 35			AS AGE
	     , 181.6		AS TALL 
	  FROM DUAL
	</select>
</mapper>